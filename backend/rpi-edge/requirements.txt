# ── ONNX Runtime (PeopleNet backend — default) ────────────────────────────
# GPU inference (CUDA 12.x + cuDNN 9.x required):
onnxruntime-gpu>=1.16.0
# CPU-only fallback — swap the two lines above/below if no GPU:
# onnxruntime>=1.16.0

# ── YOLOv8 (fallback backend) ─────────────────────────────────────────────
ultralytics>=8.0.0

# ── Computer Vision & Numerics ────────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ── Comms & Config ────────────────────────────────────────────────────────
paho-mqtt>=1.6.1
pyyaml>=6.0
